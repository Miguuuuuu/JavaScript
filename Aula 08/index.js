// Tipos de Dados Primitivos
// ~ String, number, undefined, null , boolean, symbol ~
const nome = 'Miguel'; //String
const nome1 = "Miguel"; //String
const nome2 = `Miguel`; //String
const num1 = 10; //number
const num2 = 10.52 //Number
let nomeAluno; // Undefined -> não aponta pra local nenhuma na memória
const sobrenomeAluno = null; // Nulo -> não aponta pra local nenhuma na memória
const aprovado = false; // Boolean  -> True, fale (Valores Lógicos)

let a = 2
const b = a;
console.log(a, b); // 2 e 2

a = 3;
console.log(a, b); // 3 e 2