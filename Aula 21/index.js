/*
 Operados de comparação
 > (Maior que)
 >= (Maior que ou igual a)
 < (Menor que)
 <= (Menoque que ou igual a)
 == (Igualdade) // Valor *********** (Não recomendado)
 === (Igualdade estrista) // Valor e tipo
 != (Diferente) // Valor *********** (Não recomendado)
 !== (Direferente estrito) //Valor e tipo
*/
const num1 = 10; 
const num2 = 10;
const comp = num1 !== num2;
console.log(comp);